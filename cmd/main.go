package main

import (
	c "edit-tool/internal/config"
)

func main() {
	app := c.App{}

	app.Run()

	/* name := "Divine"
	//act, val, _ := h.SplitAndGetIndices("(low, 2)")

	fmt.Println(act, val) */
}
